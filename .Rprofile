R_LIBS <- "~/R/"
options(browser = 'echo "$@" > /dev/null'  #'/usr/bin/env icecat "$@"'
      , scipen = 2
      , digits = 3
      , mc.cores = parallel::detectCores()
      , max.print = 100
      , menu.graphics = FALSE
        # , defaultPackages = c(getOption("defaultPackages"), "tidyverse", "magrittr")
        )
